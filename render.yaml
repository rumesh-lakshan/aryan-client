services:
  - type: web
    name: ARYAN-CLIENT
    env: static
    buildCommand: npm run build
    startCommand: npm run start
    staticPublishPath: /build
    routes:
      - src: /(*)
        dest: /index.html
